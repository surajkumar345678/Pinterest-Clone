import { Core } from "@kirinnee/core";
declare class MapFormatter {
    constructor(core: Core);
    FromString(val: string): Map<string, string>;
    Print(map: Map<string, string>): string;
}
export { MapFormatter };
